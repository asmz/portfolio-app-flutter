import 'package:flutter/material.dart';

class SlidePage extends StatelessWidget {
  const SlidePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('This is SlidePage', style: TextStyle(fontSize: 32.0)),
    );
  }
}
